# 参与贡献

感谢你愿意花时间。这份文档说明本项目的几条硬性约定——它们大多不是风格偏好，
而是踩过坑之后定下来的，破坏它们会造成**构建通过但功能出错**的后果。

## 开始之前

```bash
git clone https://github.com/8adss/timecapsule.git
cd timecapsule/frontend
npm install
npm run dev      # http://localhost:4113
```

提交 PR 之前，请确认这两条都通过：

```bash
npm test         # 181 个测试
npm run build    # 构建 + 静态托管的 SPA fallback
```

## 项目结构

```
frontend/src/
  domain/       纯业务逻辑，无副作用，可在 Node 里单测
  storage/      存储适配器（IndexedDB / 内存），接口可替换
  repository/   编排层：持有写入锁、保证多份数据一次落盘
  api/          页面唯一依赖的边界
  views/        页面      layouts/  布局      components/  组件
legacy/         早期后端版本，不参与构建
```

## 八条不能破坏的约定

### 1. 样式引入顺序不能改

`frontend/src/main.js` 里必须是：

```js
import 'element-plus/dist/index.css'
import './styles/theme.css'
```

`theme.css` 里有约三十条 `.el-*` 组件精修规则，与 Element Plus 自身规则**优先级相同**，
完全靠「后加载者生效」。把主题挪到前面，按钮、卡片、对话框的精修会被官方样式盖回去，
界面退化成默认蓝色调——**而构建不会报任何错**。

这也是本项目没有采用「样式按需引入」的原因，详见 `vite.config.js` 顶部注释。

### 2. 业务逻辑写在 `domain/`，保持纯函数

`domain/` 下的模块不碰存储、不碰 DOM、不引 UI 库。它们可以在 Node 里直接测试，
这是这个项目唯一有效的回归防线——**新加业务规则时请一并补测试**。

### 3. 改多份数据必须走 `writeMany`

本地没有数据库事务。`writeMany` 走的 IndexedDB 单事务是唯一的等价手段。
像「完成任务同时改任务、成就、画像」这种操作，分三次写会在中途失败时留下
「任务已完成但没有发成就」的不一致状态。

### 4. 会改数据的仓储函数必须持有写锁

仓储层的操作都是「读集合 → 改内存 → 整体写回」。两个标签页同时操作会互相覆盖，
而用户看到的是两条操作都成功了。锁由最外层的业务操作持有，内层用 `*WithinLock`
形式的函数——**Web Locks 不支持嵌套获取，内层再要一次锁会死锁**。

### 5. 导入的备份文件是不可信输入

`domain/backup.js` 处理的是外部文件，写法与仓库其他部分刻意不同：

- **字段白名单**，逐字段显式构造。不要用展开或 `Object.assign` 处理输入对象——
  漏掉一个键就是原型污染漏洞
- 每个字段都有自己的长度与取值范围上限
- **任何一处不合法就整份拒绝**，不做部分导入

### 6. 写进去的值必须满足备份校验器的上界

接着上一条。任何**能写进存储**的值，都必须落在 `domain/backup.js` 的 `LIMITS` 之内；
超了不是「显示不好看」，而是**整份导出文件永久导不回来**——校验器一处不合法就拒绝全文。

这条踩过两次，两次都不是用户手打的：

- 知识库的 `originName`（来自 `FileReader`，用户以为自己在传文件名）
- 分身的 `name`（改名时绕过了长度校验）

所以：**新增一个字段时，先问「这个值能不能从代码路径上超限」**，
而不是「输入框上有没有 `maxlength`」。UI 的 `maxlength` 挡不住程序写入。
上界与领域层的常量共用同一份数字，不要在两处各写一遍。

### 7. 语言键要用 `key: 'errors.x'` 的形式声明

`src/domain/errors.test.js` 扫的是**对象属性的形式**（`key` 后跟一个 `errors.` 开头的
字符串字面量），并且同时检查两个方向：源码里声明了但语言包里没有、
语言包里有了但源码里从未声明。

于是这些写法都会**静默漏过**检查：

- 位置参数：`new DomainError('…', { key })` —— 变量不是字面量，扫不到
- 值的形式：`const MAP = { some_code: 'errors.foo' }` —— 只认 `key:` 这个属性名
- 注释里写示例：文档里写出那个字面量，会被当成真的声明，
  于是语言包里多出一个永远用不到的死键

需要一张「错误码 → 文案键」的对照表时，写成 `{ code, key }` 的数组
（见 `domain/ai.js` 的 `AI_ERROR_KEYS`），别写成「码 → 键」的对象。

### 8. 对话链路上的三条硬约束

`functions/api/ai.js` 是全项目唯一出网的服务端代码，改它之前先读这三条：

- **浏览器只能递供应商代号，不能递 URL。** 地址由 `domain/ai.js` 的白名单决定。
  一旦允许用户自定义端点，这个函数就变成了开放代理（SSRF）——内网地址、
  云元数据服务都会变成可请求的目标
- **API Key 不进备份文件。** 备份是会被拷来拷去、贴进 issue、发给朋友的东西。
  它在 `KEY.aiConfig` 里，**刻意不在 `DATA_KEYS` 里**
- **不记录、不落盘 Key 与对话内容**，连 `console.log` 都不要加；
  上游回显的报错交给浏览器之前，先把它抹掉（`redact()`）

另外两条容易忘的部署事实：

- 项目里一旦有 `functions/`，Cloudflare Pages **默认每次请求都会调用函数**，
  静态资源也会开始消耗函数额度。`scripts/postbuild.mjs` 生成的 `_routes.json`
  就是为此存在的，**不要删**
- 产物里的 `404.html` 会让 Cloudflare 对不存在的路径返回 **404 状态码**（内容仍是应用本体），
  于是直接分享 `/app/xxx` 这类深链接时，抓取预览卡片的爬虫看到的是 404。
  它只为 GitHub Pages 而生成，在 Cloudflare 上可以把构建命令改成
  `npm run build && rm -f dist/404.html`

## 翻译与新增语言

界面文案在 `frontend/src/locales/` 下，一种语言一个 JSON 文件。

**加一门语言只要三步**：

1. 复制 `zh-CN.json`，改成目标语言的标识（如 `ja-JP.json`），翻译其中的值
2. 在 `frontend/src/i18n/index.js` 的 `SUPPORTED_LOCALES` 里加一行
3. 在同一个文件的 `messages` 里引入新文件

不需要改任何组件——语言切换器是遍历 `SUPPORTED_LOCALES` 渲染的。

几件要知道的事：

- **`fallbackLocale` 是中文。** 少翻译几个键不会让界面出现 `settings.backupTitle`
  这种东西，它会回退显示中文。所以**可以增量翻译**，不必一次翻完才提交。
- **别改键名。** 键是组件与语言包之间的唯一契约，改键等于让所有语言同时失效。
- **提交前对一下结构**：两边键数应当一致。少键会静默回退中文（不易发现），
  多键则纯粹是死代码。
- **模板里写错键名，不会有任何东西报错。** 界面会把 `knowledge.cancel` 这样的
  键名原样显示给用户，而 `npm test` 与 `npm run build` 全绿——
  `domain/errors.test.js` 只扫描 `domain/`、`repository/`、`utils/` 里声明的
  `errors.*` 键，**页面模板里的键不在它的扫描范围内**。所以改完界面文案之后，
  要手动确认新用到的键在两份语言包里都真的存在。
- **在 `domain/` 里抛错时，语言键必须写成 `{ key: 'errors.xxx' }` 这种对象形式**，
  不要当位置参数往下传。那个校验器只认 `key: '...'` 这个字面量：写成位置参数时，
  它既不会要求你补翻译（缺了会静默回退成中文），也会在你补上之后把那条翻译
  判成「语言包里多余的死键」。**两个方向都是静默的**，只有跑测试才会发现。

### 错误文案怎么国际化

`domain/` 与 `repository/` **不引入 i18n**——它们是刻意保持零依赖的纯函数层，
这两层的测试直接调用它们、直接断言中文 `message`，不必启动 Vue 或 i18n。
一旦让它们依赖翻译器，这层就得在测试里搭一套 i18n 环境。

所以错误携带的是**键 + 参数**，翻译发生在 `api/local.js`（页面唯一弹提示的地方）：

```js
throw new DomainError('任务不存在', { status: 404, key: 'errors.taskNotFound' })
```

`message` 是中文，它有两个用途：日志里能直接读懂，以及**没有 key 时的兜底文案**。
`api/local.js` 做三级回退：有 key 且当前语言里存在 → 用翻译；否则用中文 message；
再否则一句通用文案。（第一步必须先 `te()` 确认键存在，否则 vue-i18n 会把
`errors.taskNotFound` 这种原始键名直接弹给用户。）

> **新增抛出点时请一并补 `key`，并加进两份语言包。** 漏了不会报任何错，
> 英文界面只会静默显示中文。`src/domain/errors.test.js` 会扫源码把这类遗漏拦下来，
> 它同时检查「源码里有但语言包里没有」和「语言包里有但源码里没用」两个方向。
> 它认的形式与几种漏网写法见约定 7。

### 已知缺口：备份校验的细节提示

`domain/backup.js` 校验导入文件时产生的**逐字段错误**（形如
`data.tasks[0].title 应为字符串`）仍是中文。它们带字段路径与取值细节，性质更接近
诊断信息而非普通提示，且只在导入损坏文件时出现。

要国际化需要把错误收集从「拼字符串」改成「结构化问题列表、由边界层格式化」，
是一次独立改动，因此单独排期。

## 提交信息

用中文写，说明**为什么**而不只是**做了什么**。改动涉及取舍时，把被放弃的方案
和放弃的理由一并写上——这类信息在半年后比代码本身更有价值。

```
fix(M2): 校验备份文件里的主键唯一性

下游定位记录一律是 list.map(item => item.id === id ? next : item)，
id 不唯一时一次操作会改掉多行……
```

## 报告问题

请使用 Issue 模板，尽量附上：浏览器与版本、复现步骤、预期与实际结果。
如果涉及数据，**请先导出备份再操作**，并在 Issue 里说明数据结构（不要贴真实内容）。

## 关于 `legacy/`

那个目录是早期 Spring Boot + MySQL 版本的存档，**不参与构建**，
也不接受功能更新。它的价值是对照与参考，详见 `legacy/README.md`。
